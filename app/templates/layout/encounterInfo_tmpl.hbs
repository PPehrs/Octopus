{{#if loaded}}

<div class="well fz-2">
	<b>
    <div class="flex" style="text-align: center;">
        <div>{{home}}</div>
        <div class="m-l-s m-r-s">vs.</div>
        <div>{{guest}}</div>
        <div class="m-l">{{homeSets}}</div>
        <div>:</div>
        <div>{{guestSets}}</div>
    </div>
    </b>
    <div class="m-l flex clearfix" style="font-size:1.5rem;font-size:1.5vw;font-weight: normal;margin-bottom: 5px;">
		{{#if start}}
            <span class="border-5 btn-success m-r">Startzeit</span>
        	<span>{{start}}</span>
		{{/if}}
        <span class="m-l"><i>{{description}}</i></span>
        <span class="m-l"><i>(erstellt: {{date}})</i></span>
    </div>
    <div class="m-l flex clearfix" style="font-size:1.5rem;font-size:1.5vw;font-weight: normal;margin-bottom: 5px;">
            <span id="octopus_liveLeg" class="m-r" style="cursor: pointer"><a><i class="fa fa-camera-retro m-r-vs"></i><i>Live Leg</i></a></span>
        	<span style="font-size:1rem;font-size:1vw;padding-top: 0.5vw;" class="m-r"><i>(optimiert für Anzeige auf einem zusätzlichen Monitor)</i></span>
    </div>
</div>



<div class="well fz-3">
    <div class="octopus_activeMatchInfo"><div class="h3">Die Spiele werden geladen oder momentan noch kein Live-Match...</div></div>
</div>

{{else}}

<div class="flex">
<i class="fa fa-spin fa-spinner m-r"></i>
<div>Die Seite wird geladen. Normalerweise geht das schnell, vielleicht ist die Verbindung momentan nicht die schnellste...</div>
</div>

{{/if}}
